name: Refresh Google Reviews

on:
  schedule:
    - cron: '0 10 * * 0'  # Sunday 5am CDT (UTC-5)
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Fetch reviews from Google Places
        env:
          GOOGLE_SA_JSON: ${{ secrets.GOOGLE_SA_JSON }}
        run: node scripts/fetch-reviews.js

      - name: Check if reviews.json changed
        id: diff
        run: |
          if git diff --quiet data/reviews.json 2>/dev/null; then
            echo "changed=false" >> $GITHUB_OUTPUT
            echo "No change — skipping commit."
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit updated reviews.json
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name "KillerGrowth"
          git config user.email "notifications@killergrowth.com"
          git add data/reviews.json
          git commit -m "chore: refresh Google reviews [auto]"
          git push

      # NOTE: Deploy intentionally removed. Per KillerGrowth SOP-WEB-BUILD,
      # all Cloudflare Pages deploys must be run manually via wrangler.
      # Auto-deploys from GitHub Actions are banned. After reviews update,
      # run: npx wrangler pages deploy ./dist --project-name wheatland-construction --branch staging
